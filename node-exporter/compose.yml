#########
# DO NOT EDIT THIS FILE ON DIRECTLY ON WATCHDOG  SERVER!
#
# make all changes on https://github.com/medic/medic-watchdog-hosting-files/
# and then `git pull origin`
#########

services:

  prometheus:
    volumes:
      - ./node-exporter/prometheus-scrape.yml:/etc/prometheus/scrape_configs/node-exporter-scrape.yml:ro
    extra_hosts:
      - "watchdog-node-exporter:172.17.0.1"
      - "localip-node-exporter:172.17.0.1" 

